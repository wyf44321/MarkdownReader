{
  // 书籍根目录：只扫描每一项的一层子目录。
  // 某个子目录里有 config_mdr.jsonc，才会在首页显示为一本书。
  // 可写字符串（单个目录）或字符串数组（多个目录，按顺序扫描）。
  // 相对路径相对本文件所在的 MarkdownReader/；也可以写绝对路径。
  // 多个根下若出现同名书籍目录，后出现的会自动改 id，避免覆盖。
  "booksRoot": [
    "./Books"
    // , "../OtherBooks"
  ]
}
